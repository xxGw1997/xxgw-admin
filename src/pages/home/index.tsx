import { Button } from "~/components/ui/button";
import { Input } from "~/components/ui/input";

const Home = () => {
  return (
    <div>
      <Button>Click</Button>
      <Input/>
    </div>
  );
};

export default Home;
